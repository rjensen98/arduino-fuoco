#ifndef LCDButtonType_h
#define LCDButtonType_h

namespace ArduinoFuoco
{
  namespace Enums
  {
    namespace LCDButtonType
    {
      enum Enum { RIGHT = 0, UP, DOWN, LEFT, SELECT, NONE, COUNT };
    };
  }
}

#endif
